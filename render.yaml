services:
  - type: web
    name: phishing-url-detector
    runtime: python
    buildCommand: pip install --only-binary=:all: -r requirements.txt
    startCommand: gunicorn --chdir api app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
